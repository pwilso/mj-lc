Installation
===================================

The project can be run as a standalone project or in conjunction with a Synopsys Sentaurus simulation. Python is required in both cases.

Python Packages
----------------------------------

The following pacakges are required to run the Python program:
